# Serialize Library

The Serialize library provides functions for serializing and deserializing data using `std::iostream`.

## Serialize Formats

### Immutable Binary Packing



The immutable binary packing removes all labeling in the binary representation in exchange for consistent ordering of fields.

